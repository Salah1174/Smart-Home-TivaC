//salah

#include "UART.h"

// Placeholder implementations for UART (replace with actual code from your team)
void UART_SendString(const char *str) {
    while (*str) {
        // Send each character via UART (dummy implementation)
    }
}

void UART_SendInt(uint32_t num) {
    // Convert number to string and send via UART (dummy implementation)
}
